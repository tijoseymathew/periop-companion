/** Placeholder shell — the full three-column workspace lands with U1 (C5). */
export default function App() {
  return (
    <div className="h-screen flex items-center justify-center">
      <h1 className="text-lg text-ink-secondary">PeriOp Companion — Review</h1>
    </div>
  );
}
