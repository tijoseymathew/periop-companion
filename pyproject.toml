[project]
name = "periop-companion"
version = "0.1.0"
description = "Agentic peri-operative documentation assistant with claim-level provenance"
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.12,<3.14"
dependencies = [
    "nvidia-nat[adk,eval,profiler]~=1.8",
    # nvidia-nat-eval 1.8.0 imports langchain_core unconditionally but does not
    # declare it (packaging gap upstream); needed for `nat eval`.
    "langchain-core>=0.3",
    "openai>=2.44.0",
    "pydantic>=2.7",
    "python-dotenv>=1.2.2",
    "nvidia-riva-client>=2.26.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
]

[project.entry-points.'nat.components']
periop = "periop.nat.register"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/periop"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
